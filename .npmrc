; @react-three/drei requires React 19; the rest of the project is on React 18.
; The packages work fine together — we just need npm to accept the conflict.
; Required for both local `npm install` and CI/deploy installs.
legacy-peer-deps=true
